Create a new React component at `src/components/$ARGUMENTS.tsx`.

Follow the existing patterns in this codebase:
- Add `"use client"` directive only if the component needs interactivity (event handlers, hooks, browser APIs)
- Use Tailwind CSS for styling, consistent with existing components
- Add TypeScript props interface
- Export as default
